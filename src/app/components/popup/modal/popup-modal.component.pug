.popup-window(*ngIf="isVisible")((click)="onClickBackground($event)")(#background)
  .menu()
    .title {{title}}
    .message {{message}}
    .buttons(*ngFor = "let action of actions")
      .button((click) = "action.func()") {{ action.message }}
